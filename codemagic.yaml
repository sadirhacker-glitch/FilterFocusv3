workflows:
  android-build:
    name: Build FocusFilter APK
    environment:
      java: 17
    scripts:
      - name: Build APK
        script: |
          pwd
          ls -la
          find . -name "gradlew" 2>/dev/null
    artifacts:
      - FocusFilter/app/build/outputs/apk/debug/*.apk
